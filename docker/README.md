# Docker Directory

This folder can hold Docker-related support files such as:

- Environment-specific compose files
- Deployment notes
- Runtime configuration examples

The primary application container is defined in the repository root `Dockerfile`.

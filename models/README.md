# Models Directory

Use this directory for:

- Trained PyTorch checkpoints
- Exported inference artifacts
- Class labels and metadata
- Model cards and validation notes

Recommended practice:

- Version model artifacts explicitly
- Pair each model with a README or model card
- Keep training code separate from serialized weights
